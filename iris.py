# -*- coding: utf-8 -*-
"""Iris

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/17f4RVCVC7AeK5y6PZd08LSAnrnvY5W0o
"""

import io
from google.colab import files
data=files.upload()

import pandas as pd
dataset=pd.read_csv(io.BytesIO(data['Iris.csv']))

#dataset.describe()

dataset=dataset.drop('Id',axis='columns')
#dataset.info()

#dataset['Species'].value_counts()

import seaborn as sns
#sns.pairplot(dataset,hue='Species',markers='o')

#dataset

independent=dataset.drop('Species',axis='columns')
dependent=dataset.iloc[:,4:]

from sklearn.linear_model import LogisticRegression
regr=LogisticRegression()

from sklearn.model_selection import train_test_split
independent_train,independent_test,dependent_train,dependent_test=train_test_split(independent,dependent,test_size=0.33)

regr.fit(independent_train,dependent_train)

pred=regr.predict(independent_test)
pred=pd.DataFrame(pred)
pred

from sklearn.metrics import confusion_matrix
cm=confusion_matrix(dependent_test,pred)
#sns.heatmap(cm,annot=True,linewidths=0.06)

acc=(14+15+10)/(14+15+10+1+1)

#print('accuracy=',acc*100)

#independent_test

regr.predict([[5.3,6.2,3.3,5.3]])

print("Enter your SepalLength 	SepalWidth 	PetalLength	PetalWidth in centimeteres one by one	")
a=input("enter Sepal length ")
b=input("enter Sepal width ")
c=input("enter petal length ")
d=input("enter petal width ")

a=float(a)
b=float(b)
c=float(c)
d=float(d)

val=regr.predict([[a,b,c,d]])

print('According to your given data, flower is',*val)

